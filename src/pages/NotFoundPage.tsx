import PageShell from '../components/PageShell';

function NotFoundPage() {
    return (
        <PageShell
            title="not found"
            subtitle="lost"
        />
    );
}

export default NotFoundPage;
